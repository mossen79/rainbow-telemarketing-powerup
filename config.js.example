// Configuration file for Trello Power-Up
// Copy this file to config.js and fill in your values

const CONFIG = {
    // Trello App Key - Get yours at: https://trello.com/app-key
    // IMPORTANT: Keep this file PRIVATE - do not commit to public repos!
    TRELLO_APP_KEY: 'YOUR_TRELLO_API_KEY_HERE',
    
    // App metadata
    APP_NAME: 'Rainbow Telemarketing Scripts',
    APP_VERSION: '1.1.0',
    
    // Feature flags (enable/disable features)
    FEATURES: {
        SAVE_TO_TRELLO: true,           // Save conversation history to Trello comments
        KEYBOARD_SHORTCUTS: true,        // Enable keyboard shortcuts (1,2,3,4)
        MULTI_CAMPAIGN: false,           // Multiple campaign selector (future feature)
        AUTO_FILL_FROM_CARD: false       // Auto-fill lead name from card (future feature)
    },
    
    // UI Configuration
    UI: {
        NOTIFICATION_DURATION: 5000,     // How long notifications show (ms)
        AUTO_CLOSE_ON_SUCCESS: false,    // Close modal automatically after success
        SHOW_DEBUG_INFO: false           // Show debug information in console
    }
};

// Export for use in other files
if (typeof module !== 'undefined' && module.exports) {
    module.exports = CONFIG;
}


